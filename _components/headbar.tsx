"use client";

export default function HeadBar() {
  return (
    <div>
    <div
      style={{
        width: "100%",
        height: "4px",
        background:
          "linear-gradient(to right, red, yellow, green, blue)",
      }}
    />
    </div>
  );
}
